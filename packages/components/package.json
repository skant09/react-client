{
  "name": "components",
  "version": "1.0.0",
  "description": "",
  "main": "build/index",
  "scripts": {
    "prepublish": "npx babel src --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-react": "^6.24.1"
  }
}
